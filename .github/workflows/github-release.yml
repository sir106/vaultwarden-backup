name: 'GitHub Release'

on:
  push:
    tags:
      - 'v*.*.*'
      - '!v*.*.*-*'

permissions:
  contents: write

jobs:
  release:
    name: Create Release

    runs-on: ubuntu-latest

    steps:
      -
        name: Checkout
        uses: actions/checkout@v7
      -
        name: Release
        uses: softprops/action-gh-release@v3
        with:
          body: |
            [CHANGELOG](https://github.com/sir106/vaultwarden-backup/blob/main/CHANGELOG.md)
